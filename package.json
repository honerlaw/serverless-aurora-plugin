{
  "name": "serverless-aurora-plugin",
  "version": "1.2.0",
  "description": "A simple serverless framework plugin to create an aurora serverless database",
  "main": "index.js",
  "scripts": {
    "prepublish": "npm run build",
    "clean": "rm -rf ./*.js ./*.js.map ./bin/ ./.serverless/ ./resource/",
    "build": "npm run clean && tsc -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Derek Honerlaw",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.2.1"
  }
}
